# Generated by tools/render_controls.py — edit the data files, not this file.
#
# Rule:        CKV_AWS_68 (checkov 3.3.16)
# Applies to:  aws_cloudfront_distribution
# Read with:   aws_cloudfront_distributions -> aws_cloudfront_distribution (stock inspec-aws, no custom reader)
#
# The rule id is the identity: file name, control id and `tag checkov_id` all
# carry it, and tools/lint_catalog_drift.py asserts the three agree.

exempt = (input('exempt_assets') || {})['CKV_AWS_68'] || []

control 'CKV_AWS_68' do
  title 'CloudFront Distribution should have WAF enabled'

  desc <<~DESC
    Checkov asserts this against Terraform. This profile asserts it against
    the aws_cloudfront_distribution resources that actually exist, read
    through the stock inspec-aws aws_cloudfront_distribution resource.
  DESC

  desc 'rationale', <<~RATIONALE
    CloudFront Distribution should have WAF enabled. Anchors derived from
    the check's application security category, not reviewed control by
    control.
  RATIONALE

  desc 'check', <<~CHECK
    Checkov looks for: aws_cloudfront_distribution: web_acl_id is CKV_ANY
  CHECK

  desc 'fix', <<~'FIX'
    See https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/cloudfront_distribution#web-acl-id
  FIX

  tag checkov_id:            'CKV_AWS_68'
  tag checkov_category:      'APPLICATION_SECURITY'
  tag checkov_version:       '3.3.16'
  tag checkov_kind:          'value'
  tag tf_resources:          %w[aws_cloudfront_distribution]
  tag tf_docs:               'https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/cloudfront_distribution#web-acl-id'
  tag nist:                  ['SA-11', 'SI-10']
  tag nist_r4:               ['SA-11', 'SI-10']
  tag cci:                   ['CCI-003173']
  tag ksi:                   ['KSI-SCR-MIT']
  tag severity:              'medium'
  tag severity_source:       'assessed'
  tag nist_source:           'category-derived'
  tag implementation_status: 'implemented'

  # Enumerated at control scope, then each asset asserted on its own. The
  # resource is an ARGUMENT to `describe`, which evaluates on the control --
  # calling it inside the block would defer it into the example.
  ids = aws_cloudfront_distributions.distribution_ids
  in_scope = ids.reject { |id| checkov_exempt?(id: id, type: 'aws_cloudfront_distribution', rules: exempt) }

  applicable = !in_scope.empty?
  impact 0.5
  impact 0.0 unless applicable
  only_if('no aws_cloudfront_distribution in scope') { applicable }

  in_scope.each do |id|
    describe aws_cloudfront_distribution(distribution_id: id) do
      its('web_acl_id') { should_not be_empty }
    end
  end
end

# How a Terraform resource type becomes something this profile can assess.
#
# Keyed by (check, terraform type) rather than by check, because the argument a
# check inspects varies by type -- CKV_AWS_88 reads associate_public_ip_address
# on an instance and network_interfaces[0].associate_public_ip_address on a
# launch template. A map keyed only by check would inspect the wrong field for
# one of them and still look right.
#
# reader    the InSpec resource that enumerates every deployed asset of this type
# field     the row attribute answering this check
# satisfies how the field is judged: equals / not_equals / empty / any_of
# note      only where the deployed assertion is not a restatement of the IaC one
#
# Seeded with the aws_instance tranche. Everything else in the catalogue is
# unmapped, which is what tools/lint_catalog_drift.py counts -- an unmapped
# check has no control, and that is visible rather than assumed.

readers:
  aws_instance:              {resource: aws_compute_assets, type: aws_instance}
  aws_launch_template:       {resource: aws_compute_assets, type: aws_launch_template}
  aws_launch_configuration:  {resource: aws_compute_assets, type: aws_launch_configuration}

checks:
  CKV_AWS_79:
    aws_instance:             {field: imds_tokens_or_disabled, satisfies: equals, value: true}
    aws_launch_template:      {field: imds_tokens_or_disabled, satisfies: equals, value: true}
    aws_launch_configuration: {field: imds_tokens_or_disabled, satisfies: equals, value: true}

  CKV_AWS_88:
    aws_instance:        {field: public_ip, satisfies: equals, value: false}
    aws_launch_template: {field: public_ip, satisfies: equals, value: false}

  CKV_AWS_8:
    aws_instance:
      field: unencrypted_volumes
      satisfies: empty
      note: >-
        Stronger than the IaC check. Checkov sees the block devices declared in
        HCL; DescribeVolumes sees every volume actually attached, including ones
        attached later by hand, outside Terraform.
    aws_launch_configuration: {field: unencrypted_volumes, satisfies: empty}

  CKV_AWS_126:
    aws_instance: {field: detailed_monitoring, satisfies: equals, value: true}

  CKV_AWS_135:
    aws_instance: {field: ebs_optimized, satisfies: equals, value: true}

  CKV_AWS_46:
    aws_instance:             {field: user_data_secrets, satisfies: empty}
    aws_launch_template:      {field: user_data_secrets, satisfies: empty}
    aws_launch_configuration: {field: user_data_secrets, satisfies: empty}

  # --- S3, read through stock inspec-aws resources (no custom reader) ---
  CKV_AWS_18:
    aws_s3_bucket:
      reader: stock
      enumerate: {resource: aws_s3_buckets, ids: bucket_names}
      assert:
        resource: aws_s3_bucket
        arg: bucket_name
        property: "has_access_logging_enabled?"
        satisfies: equals
        value: true
  CKV_AWS_19:
    aws_s3_bucket:
      reader: stock
      enumerate: {resource: aws_s3_buckets, ids: bucket_names}
      assert:
        resource: aws_s3_bucket
        arg: bucket_name
        property: "has_default_encryption_enabled?"
        satisfies: equals
        value: true
  CKV_AWS_21:
    aws_s3_bucket:
      reader: stock
      enumerate: {resource: aws_s3_buckets, ids: bucket_names}
      assert:
        resource: aws_s3_bucket
        arg: bucket_name
        property: "has_versioning_enabled?"
        satisfies: equals
        value: true
  CKV_AWS_20:
    aws_s3_bucket:
      reader: stock
      enumerate: {resource: aws_s3_buckets, ids: bucket_names}
      assert:
        resource: aws_s3_bucket
        arg: bucket_name
        property: "public?"
        satisfies: equals
        value: false
  CKV_AWS_379:
    aws_s3_bucket:
      reader: stock
      enumerate: {resource: aws_s3_buckets, ids: bucket_names}
      assert:
        resource: aws_s3_bucket
        arg: bucket_name
        property: "has_secure_transport_enabled?"
        satisfies: equals
        value: true
  CKV2_AWS_6:
    aws_s3_bucket:
      reader: stock
      enumerate: {resource: aws_s3_buckets, ids: bucket_names}
      assert:
        resource: aws_s3_bucket
        arg: bucket_name
        property: "prevent_public_access?"
        satisfies: equals
        value: true
  CKV2_AWS_61:
    aws_s3_bucket:
      reader: stock
      enumerate: {resource: aws_s3_buckets, ids: bucket_names}
      assert:
        resource: aws_s3_bucket
        arg: bucket_name
        property: "bucket_lifecycle_rules"
        satisfies: not_empty

  # --- RDS instances, read through stock inspec-aws (create_resource_methods
  #     exposes every DescribeDBInstances field as a method) ---
  CKV_AWS_16:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "storage_encrypted"
        satisfies: equals
        value: true
  CKV_AWS_17:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "publicly_accessible"
        satisfies: equals
        value: false
  CKV_AWS_157:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "multi_az"
        satisfies: equals
        value: true
  CKV_AWS_161:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "iam_database_authentication_enabled"
        satisfies: equals
        value: true
  CKV_AWS_226:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "auto_minor_version_upgrade"
        satisfies: equals
        value: true
  CKV_AWS_293:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "deletion_protection"
        satisfies: equals
        value: true
  CKV_AWS_353:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "performance_insights_enabled"
        satisfies: equals
        value: true
  CKV_AWS_354:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "performance_insights_kms_key_id"
        satisfies: not_empty
  CKV_AWS_129:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "enabled_cloudwatch_logs_exports"
        satisfies: not_empty
  CKV_AWS_118:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "monitoring_interval"
        satisfies: greater_than
        value: 0
  CKV_AWS_211:
    aws_db_instance:
      reader: stock
      enumerate: {resource: aws_rds_instances, ids: db_instance_identifiers}
      assert:
        resource: aws_rds_instance
        arg: positional
        property: "ca_certificate_identifier"
        satisfies: in_list
        value: ['rds-ca-rsa2048-g1', 'rds-ca-rsa4096-g1', 'rds-ca-ecc384-g1']

  # --- Aurora/RDS clusters, Redshift, ElastiCache (stock inspec-aws) ---
  CKV_AWS_96:
    aws_rds_cluster:
      reader: stock
      enumerate: {resource: aws_rds_clusters, ids: cluster_identifier}
      assert:
        resource: aws_rds_cluster
        arg: db_cluster_identifier
        property: "storage_encrypted"
        satisfies: equals
        value: true
  CKV_AWS_139:
    aws_rds_cluster:
      reader: stock
      enumerate: {resource: aws_rds_clusters, ids: cluster_identifier}
      assert:
        resource: aws_rds_cluster
        arg: db_cluster_identifier
        property: "deletion_protection"
        satisfies: equals
        value: true
  CKV_AWS_162:
    aws_rds_cluster:
      reader: stock
      enumerate: {resource: aws_rds_clusters, ids: cluster_identifier}
      assert:
        resource: aws_rds_cluster
        arg: db_cluster_identifier
        property: "iam_database_authentication_enabled"
        satisfies: equals
        value: true
  CKV_AWS_313:
    aws_rds_cluster:
      reader: stock
      enumerate: {resource: aws_rds_clusters, ids: cluster_identifier}
      assert:
        resource: aws_rds_cluster
        arg: db_cluster_identifier
        property: "copy_tags_to_snapshot"
        satisfies: equals
        value: true
  CKV_AWS_324:
    aws_rds_cluster:
      reader: stock
      enumerate: {resource: aws_rds_clusters, ids: cluster_identifier}
      assert:
        resource: aws_rds_cluster
        arg: db_cluster_identifier
        property: "enabled_cloudwatch_logs_exports"
        satisfies: not_empty
  CKV_AWS_327:
    aws_rds_cluster:
      reader: stock
      enumerate: {resource: aws_rds_clusters, ids: cluster_identifier}
      assert:
        resource: aws_rds_cluster
        arg: db_cluster_identifier
        property: "kms_key_id"
        satisfies: not_empty
  CKV_AWS_326:
    aws_rds_cluster:
      reader: stock
      enumerate: {resource: aws_rds_clusters, ids: cluster_identifier}
      assert:
        resource: aws_rds_cluster
        arg: db_cluster_identifier
        property: "backtrack_window"
        satisfies: greater_than
        value: 0
  CKV_AWS_64:
    aws_redshift_cluster:
      reader: stock
      enumerate: {resource: aws_redshift_clusters, ids: cluster_identifiers}
      assert:
        resource: aws_redshift_cluster
        arg: cluster_identifier
        property: "encrypted"
        satisfies: equals
        value: true
  CKV_AWS_87:
    aws_redshift_cluster:
      reader: stock
      enumerate: {resource: aws_redshift_clusters, ids: cluster_identifiers}
      assert:
        resource: aws_redshift_cluster
        arg: cluster_identifier
        property: "publicly_accessible"
        satisfies: equals
        value: false
  CKV_AWS_141:
    aws_redshift_cluster:
      reader: stock
      enumerate: {resource: aws_redshift_clusters, ids: cluster_identifiers}
      assert:
        resource: aws_redshift_cluster
        arg: cluster_identifier
        property: "allow_version_upgrade"
        satisfies: equals
        value: true
  CKV_AWS_321:
    aws_redshift_cluster:
      reader: stock
      enumerate: {resource: aws_redshift_clusters, ids: cluster_identifiers}
      assert:
        resource: aws_redshift_cluster
        arg: cluster_identifier
        property: "enhanced_vpc_routing"
        satisfies: equals
        value: true
  CKV_AWS_343:
    aws_redshift_cluster:
      reader: stock
      enumerate: {resource: aws_redshift_clusters, ids: cluster_identifiers}
      assert:
        resource: aws_redshift_cluster
        arg: cluster_identifier
        property: "automated_snapshot_retention_period"
        satisfies: greater_than
        value: 0
  CKV_AWS_320:
    aws_redshift_cluster:
      reader: stock
      enumerate: {resource: aws_redshift_clusters, ids: cluster_identifiers}
      assert:
        resource: aws_redshift_cluster
        arg: cluster_identifier
        property: "db_name"
        satisfies: not_empty
  CKV_AWS_29:
    aws_elasticache_replication_group:
      reader: stock
      enumerate: {resource: aws_elasticache_replication_groups, ids: ids}
      assert:
        resource: aws_elasticache_replication_group
        arg: replication_group_id
        property: "at_rest_encryption_enabled"
        satisfies: equals
        value: true
  CKV_AWS_30:
    aws_elasticache_replication_group:
      reader: stock
      enumerate: {resource: aws_elasticache_replication_groups, ids: ids}
      assert:
        resource: aws_elasticache_replication_group
        arg: replication_group_id
        property: "transit_encryption_enabled"
        satisfies: equals
        value: true
  CKV_AWS_191:
    aws_elasticache_replication_group:
      reader: stock
      enumerate: {resource: aws_elasticache_replication_groups, ids: ids}
      assert:
        resource: aws_elasticache_replication_group
        arg: replication_group_id
        property: "kms_key_id"
        satisfies: not_empty
  CKV_AWS_134:
    aws_elasticache_replication_group:
      reader: stock
      enumerate: {resource: aws_elasticache_replication_groups, ids: ids}
      assert:
        resource: aws_elasticache_replication_group
        arg: replication_group_id
        property: "snapshot_retention_limit"
        satisfies: greater_than
        value: 0
